const loops = {
    forLoop : `for (let i = 0; i < 5; i++) {
    console.log("Itération numéro " + i);
    }`,

    whileLoop : `let i = 0;
    while (i < 5) {
    console.log("Itération numéro " + i); i++;
    }`
}

export default loops;